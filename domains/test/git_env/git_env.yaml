# A simple git env import

name: "git_env"
category: "Zerg Tests"
description: |
  Test to see that we can ingress a complete git/github repository correctly

specs: []

tests: []

references: 
  - description: "Python dot-env for simple testing (small 3K SLOC repository)"
    git_url: "https://github.com/theskumar/python-dotenv"
    environment_path: "repos/python-dotenv"
    format: "git"
    git_config:
      ref: "main"  
    required: true
    read_only: true

configs: []